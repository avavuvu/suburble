// Re-export from shared package - this is the single source of truth
import type { TrainLineName } from "suburble-shared"
import { trainLineColorMap } from "suburble-shared"

export default trainLineColorMap